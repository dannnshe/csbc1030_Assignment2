
export const calculateArea = (length, width) => {
    return length*width;
}

export const calculateVolume = (length, width, height) => {
    return length*width*height;
}

export const calculateSquare = (length) => {
    return length**2;
}