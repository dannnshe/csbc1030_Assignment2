const {calculateArea, calculateSquare, calculateVolume} = require('./calculate')

console.log(`Area of Figure ${calculateArea(4, 2)}`);

console.log(`Volume of Figure ${calculateVolume(2, 2, 2)}`); 

console.log(`Square of Figure ${calculateSquare(4)}`);